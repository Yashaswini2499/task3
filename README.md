# task3
Task 3 focuses on extracting data from the **LibraryDB** using SQL queries. It demonstrates `SELECT *` and column-specific queries, applies conditions with `WHERE`, `AND`, `OR`, `LIKE`, `BETWEEN`, sorts results using `ORDER BY`, and limits rows with `LIMIT` to refine and present meaningful outputs.
